<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.forecastx">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ContextActivity" />
        <activity android:name=".EvidenceActivity" />
        <activity android:name=".DateFrequencyActivity" />
        <activity android:name=".ReharassmentActivity" />
        <activity android:name=".PaymentActivity" />
        <activity android:name=".RespondentParticularActivity" />
        <activity android:name=".PersonalServiceActivity" />
        <activity android:name=".PtcActivity" />
        <activity android:name=".CourtHearingActivity" />
        <activity android:name=".ProtectionType" />
        <activity android:name=".EpoHistoryActivity" />
        <activity android:name=".FinalReportActivity"></activity>
    </application>

</manifest>